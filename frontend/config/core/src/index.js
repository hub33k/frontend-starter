import paths from './paths';
import * as config from './config';
import env from './env';
import * as postcssConfig from './postcss.config';

export default {
  paths,
  config,
  env,
  postcssConfig,
};
